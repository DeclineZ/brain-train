import VitalmindLaunchContent from "./content";

export const metadata = {
  referrer: "no-referrer",
  robots: { index: false, follow: false },
};

export default function VitalmindLaunchPage() {
  return <VitalmindLaunchContent />;
}
